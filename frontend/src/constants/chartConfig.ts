/**
 * Shared chart configuration constants.
 */

/** Disable Recharts animations when datasets exceed this many data points. */
export const CHART_ANIMATION_THRESHOLD = 500

/** Standard progress bar height class. Use this for all progress bars. */
export const PROGRESS_BAR_HEIGHT = 'h-2'

/** Standard section icon size. */
export const SECTION_ICON_SIZE = 'w-5 h-5'
